attributes = {
  id: user.id,
  email: user.email,
  username: user.username,
  phone: user.phone,
  created_at: user.created_at.as_json,
  updated_at: user.updated_at.as_json,
  terms_accepted_at: user.terms_accepted_at.as_json,
  user_type: user.user_type,
}

{ user: attributes }
