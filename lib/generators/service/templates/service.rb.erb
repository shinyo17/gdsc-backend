class <%= service_class_name %>
  attr_reader :args
  def initialize(args)
    @args = args
  end

  def self.call(*args, &block)
    new(*args, &block).call
  end

  def call
    result
  end

  private

  def result
    "hello <%= service_class_name %> : #{args}"
  end
end